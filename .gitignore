# ============================================================
# PenScope —— Git 忽略规则
# 原则：源码与可再生的资源入库；密钥、用户数据、日志、调试/训练产物、构建产物一律不入库。
# ============================================================

# ---- 构建产物（exe / PyInstaller 临时） ----
dist/
build/
*.spec.bak
# 发布用的 exe 走 GitHub Release 资产，不入 git 历史
*.exe

# ---- 运行时用户数据（含扫描目标、发现、日志） ----
*.db
*.db-shm
*.db-wal
autopentest.db
autopentest.db-shm
autopentest.db-wal
autopentest.log
*.log
npm-debug.log*
yarn-debug.log*

# ---- 密钥 / 凭据 / 敏感配置（严禁入库） ----
*.key
autopentest.key
autopentest_vault.json
*vault*.json
.env
.env.*
# 提示：运行时密钥由程序在 exe 同级目录自动生成（Fernet），前端永不拿到明文密码。
# 切勿将 AUTOPENTEST_VAULT_KEY 或任何真实凭据提交到仓库。

# ---- 调试 / 训练 / 预发布产物（仅仓库根目录的一次性文件，勿匹配 .github 模板） ----
/bug_report.json
/bug_report.md
/fix_proposal.json
/_u04_transform.py
knowledge_feed/
frontend/prototypes/
.workbuddy/

# ---- 实战 / 一次性挖洞 scratch 脚本与运行产物（仅仓库根目录；非产品代码，勿跟踪） ----
# 手动挖洞 / SRC 验证的一次性驱动脚本及其带时间戳的 JSON 产物；忽略以防 `git status`
# 噪声与误 `git add .` 卷入。产品模块（scanner/ 下的 weak_pass/product_fp/product_intel、
# scanner/data/、tests/ 等）不在此列，正常入库。
/_manual_hunt*.py
/_manual_hunt_*.json
/_manual_report.py
/_src_*.py
/_src_*.json
/_enum_lncc_subs.py
/_lncc_subs_*.json
/pen_xval_lncc.py
/pen_xval_lncc.json
/run_lncc_subs.py
/verify_oa_leads.py
/verify_oa_leads.json
/verify_p5_live.py

# ---- 二进制资源（由 make_icon.py 在构建时再生，见 build_nowrap.py） ----
assets/icon.ico
assets/tray.png

# ---- Python ----
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/

# ---- 系统 / 编辑器 ----
.DS_Store
Thumbs.db
.idea/
.vscode/
