# Changelog

本文件记录 PenScope **公开发布**的版本变更。内部开发期（v1.x 迭代）的各项能力已整合进 `1.0.0` 正式版；更细的开发日志见仓库 README 的版本演进表。

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)：`MAJOR.MINOR.PATCH`。
`MAJOR`=不兼容或重大架构变更；`MINOR`=向后兼容的新功能；`PATCH`=向后兼容的问题修复。
发布标签为 `v<版本号>`，GitHub Release 自动构建并挂载 `PenScope.exe` 作为下载资产。

---

## [v1.0.0] - 2026-08-03

首个公开正式版。纯本地、无后端的**授权式**自动化渗透测试桌面工具（Windows）。

### 功能
- **授权围栏**：新增目标必须显式确认「已获书面授权」才会被批准可扫描，防止对未授权资产发起测试。
- **人工复核闸门**：利用验证（时间盲注证明）、文件上传测试等高危动作会被暂停，需人工点击「批准」才继续执行。
- **完整工作流**：`授权目标 → 端口/子域探测 → Web 漏洞扫描 → 利用验证 → 报告`，一键生成带风险分级与修复建议的 HTML 报告。
- **检测能力**：
  - 端口/服务探测与资产暴露发现；
  - Web 十大类漏洞：SQL 注入、XSS、CSRF、文件上传、SSRF（CWE-918）、路径遍历（CWE-22）、开放重定向（CWE-601）、缺失授权（CWE-862）、认证缺陷（CWE-287，默认凭据探测 opt-in）、敏感信息泄露；
  - 邮件协议 STARTTLS / 明文传输检测（SMTP/POP3/IMAP，只观察不登录）；
  - 目录暴露与云 AK/SK、JWT、私钥、内网 IP、调试堆栈等泄露检测（全只读）；
  - 已知漏洞版本联动（被动命中「组件+版本+CVE」）。
- **证据与降噪**：结构化漏洞模型 + 语义去重；证据分级 L1–L4；请求/响应 Diff；CVSS 3.1 交互计算器（人工校准回写）。
- **报告与导出**：多模板 HTML 报告（摘要/技术/合规）、SARIF 2.1.0 与标准化 JSON 机器可读导出、漏洞利用链编排（SSRF→内网 等）、中文渗透测试授权书生成。
- **可视化**：资产拓扑力导向图、漏洞矩阵、扫描对比基线 Diff、阶段甘特图。
- **工程能力**：插件化框架（`~/.autopentest/plugins/` 自动发现）、弱口令字典外置、资产变更告警、敏感数据脱敏、定时/批量巡检、审计日志、中英双语界面与报告。

### 安全
- 默认**只读、不爆破、不写**；主动动作（默认凭据探测、会话续期、上传测试）默认关闭，需人工开启并二次确认。
- 运行时凭据保险库密钥由程序在 exe 同级目录**自动生成**（Fernet），前端 `get_auth_profile` 只返回掩码，后端绝不回传明文密码。
- 仓库经 `.gitignore` 排除所有 `*.key`、`*vault*.json`、数据库（`*.db*`）与日志（`*.log`）；发布产物（源码与 exe）**不含任何私人信息、密钥或敏感凭据**。

### 发布
- 语义化版本管理（`tools/release.py` 自动递增 `config.py` 的 `VERSION`）。
- CI 自动发布（`.github/workflows/release.yml`）：推送 `main` 或 `v*` 标签即构建 `PenScope.exe` 并作为 GitHub Release 资产公开。
