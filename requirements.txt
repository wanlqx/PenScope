# PenScope —— 无后端桌面工具依赖
# 不需要 Flask / Werkzeug（已改为 pywebview 本地前端 + JS 桥调用本地 Python 引擎）
requests>=2.31
pywebview>=5.0
pystray>=0.19
Pillow>=10.0
cryptography>=50.0.1   # scanner.vault / session_renew 硬依赖（Fernet 加密凭据保险库）
pyinstaller>=6.0   # 仅打包时需要
# weasyprint>=60.0  # 可选：如需在应用内直接生成 PDF 报告，安装后可启用（较重，Windows 需额外 GTK 依赖）
# pdfkit>=1.0.0     # 可选：另一种 PDF 生成方案（需系统安装 wkhtmltopdf）
